namespace BE.Models
{
    public enum UserRole { Admin, Manager, Member }
    public enum WorkStatus { Todo, InProgress, Review, Done, Canceled }
    public enum TaskPriority { Low, Medium, High, Urgent }
}